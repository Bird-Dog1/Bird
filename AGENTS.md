# Agents

## Cursor Cloud specific instructions

This repository (`Bird`) is currently an empty project with only a `README.md`. There are no services, dependencies, build tools, or test frameworks configured yet.

- **No dependencies to install** — no `package.json`, `requirements.txt`, `go.mod`, or similar.
- **No build/lint/test commands** — no build system or test framework is configured.
- **No services to run** — no application server, database, or other infrastructure.

When application code is added in the future, update this file with relevant development instructions (how to install deps, run services, lint, test, build).
