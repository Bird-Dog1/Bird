# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Bird") is currently an empty project with only a `README.md`. There is no application code, no dependencies, no build system, and no services to run yet.

### Current state

- No package manager configured (no `package.json`, `requirements.txt`, `go.mod`, etc.)
- No source code files
- No tests, linting, or build tooling
- No Docker or service infrastructure

### When code is added

Future agents should re-evaluate the repository structure and update this file with:
- How to install dependencies
- How to run lint/test/build
- How to start services for development
- Any non-obvious caveats discovered during setup
